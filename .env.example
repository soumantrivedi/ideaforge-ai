# API Key Encryption Key (REQUIRED for API key encryption/decryption)
# Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# IMPORTANT: This must be the same value used when keys were originally encrypted
# If you change this, all encrypted API keys in the database will become unusable
API_KEY_ENCRYPTION_KEY=your-encryption-key-here

# Other environment variables...
