# IdeaForge AI - Docker Compose Fallback Environment Variables
# This file contains Docker Compose-specific configuration
# Used as fallback alternative (not preferred)
# Load via: docker-compose up (reads from .env file)

# ============================================================================
# Platform-Specific Configuration (Docker Compose)
# ============================================================================
# Database password (dev password is fine for local)
POSTGRES_PASSWORD=devpassword
# Full database URL
DATABASE_URL=postgresql://agentic_pm:devpassword@postgres:5432/agentic_pm_db

# ============================================================================
# Frontend Configuration (Docker Compose)
# ============================================================================
# Direct backend access (no nginx proxy in docker-compose)
VITE_API_URL=http://localhost:8000
# Frontend URL (exposed on port 3001)
FRONTEND_URL=http://localhost:3001
# CORS allowed origins for docker-compose
CORS_ORIGINS=http://localhost:3001,http://localhost:3000,http://localhost:5173

