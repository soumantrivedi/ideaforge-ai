# IdeaForge AI - Kind Local Development Environment Variables
# This file contains Kind-specific configuration for local development
# Load this into ConfigMap using: kustomize build k8s/overlays/kind | kubectl apply -f -
# Or use: make kind-load-config

# ============================================================================
# Platform-Specific Configuration (Kind)
# ============================================================================
# Database Configuration
# IMPORTANT: These values must match what's in k8s/kind/configmap.yaml
POSTGRES_USER=agentic_pm
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=agentic_pm_db

# ============================================================================
# Frontend Configuration (Kind)
# ============================================================================
# Use relative paths for cloud-native service-to-service communication
# Frontend nginx proxies /api requests to backend service internally
VITE_API_URL=
# Frontend URL via ingress (or use http://ideaforge.local if using hostnames)
FRONTEND_URL=http://localhost:80
# CORS allowed origins for local development
CORS_ORIGINS=http://localhost,http://localhost:80,http://localhost:8080,http://ideaforge.local,http://api.ideaforge.local

# ============================================================================
# Namespace (Kind)
# ============================================================================
K8S_NAMESPACE=ideaforge-ai

# ============================================================================
# McKinsey SSO Configuration (Kind - Local Development)
# ============================================================================
# For local development, use test credentials or disable McKinsey SSO
# Set MCKINSEY_CLIENT_ID to empty string to disable McKinsey SSO in local dev
MCKINSEY_CLIENT_ID=
MCKINSEY_CLIENT_SECRET=
MCKINSEY_REDIRECT_URI=http://localhost:80/api/auth/mckinsey/callback
# Generate test encryption key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
MCKINSEY_TOKEN_ENCRYPTION_KEY=

